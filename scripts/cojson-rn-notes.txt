cojson -> react-native install footprint (MaiaOS)

Chain (see: bun why react-native):
  cojson@0.20.16 -> cojson-core-rn@0.20.16 (peer) -> react-native@0.82.x

Local mitigation:
  - Do NOT use Bun overrides to stub cojson-core-rn unless upstream confirms Node never loads it at runtime.
  - Prefer upgrading cojson when upstream splits optional RN, or open an upstream issue.

Upstream-compatible: yes — no fork; pinning cojson@0.20.16 is already in package.json files.
