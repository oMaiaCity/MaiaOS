{
  "name": "maia-city",
  "version": "0.1.23",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "cd ../.. && bun run bundles:build && cd services/maia-city && vite build",
    "preview": "vite preview",
    "start": "bun server.js",
    "deploy": "cd ../.. && flyctl deploy --dockerfile services/maia-city/Dockerfile --config services/maia-city/fly.toml"
  },
  "dependencies": {
    "@MaiaOS/kernel": "workspace:*",
    "@MaiaOS/vibes": "workspace:*",
    "buffer": "^6.0.3"
  },
  "devDependencies": {
    "vite": "^7.2.4"
  }
}
