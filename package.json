{
	"name": "MaiaOS",
	"private": true,
	"workspaces": [
		"services/app",
		"services/sync",
		"libs/*"
	],
	"type": "module",
	"scripts": {
		"dev": "bun --silent scripts/dev.js",
		"dev:app": "bun scripts/dev-app.js",
		"dev:sync": "bun scripts/dev-sync.js",
		"deploy": "./scripts/deploy-all.sh",
		"deploy:app": "cd services/app && ./deploy.sh",
		"deploy:app:verify": "bun scripts/fly-build-args-verify.js",
		"deploy:sync": "cd services/sync && ./deploy.sh",
		"avens:build": "cd libs/maia-avens && bun run build",
		"distros:build": "bun run --filter @MaiaOS/maia-distros build",
		"docs:generate": "bun scripts/generate-llm-docs.js",
		"docs:watch": "bun scripts/generate-llm-docs.js --watch",
		"version:sync": "node scripts/sync-version.js",
		"agent:generate": "bun libs/maia-self/scripts/generate-credentials.js",
		"deploy:secrets": "bun scripts/fly-secrets-sync.js",
		"format": "bun run format:maia && bunx biome format --write .",
		"format:maia": "bun scripts/format-maia.js",
		"format:maia:check": "bun scripts/format-maia.js --check",
		"lint": "bunx biome lint .",
		"check": "bunx biome check .",
		"check:fix": "bunx biome check --write .",
		"check:ci": "bun run format:maia:check && bunx biome ci .",
		"prepare": "husky"
	},
	"devDependencies": {
		"@biomejs/biome": "2.3.8",
		"@playwright/test": "^1.58.2",
		"@realfavicongenerator/generate-favicon": "^0.6.0",
		"@realfavicongenerator/image-adapter-node": "^0.6.1",
		"husky": "^9.1.7",
		"lint-staged": "^16.2.7"
	},
	"lint-staged": {
		"*.{js,css,html,md,json}": "biome check --write --no-errors-on-unmatched",
		"*.maia": "bun scripts/format-maia.js"
	},
	"dependencies": {
		"@electric-sql/pglite": "^0.3.15"
	}
}
