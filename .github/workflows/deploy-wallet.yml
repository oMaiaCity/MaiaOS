name: Deploy Wallet Service to Fly.io

on:
  workflow_run:
    workflows: ["Release"]
    types:
      - completed
    branches:
      - main
  workflow_dispatch: # Allow manual trigger

jobs:
  deploy-wallet:
    name: Deploy Wallet
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    environment: PRODUCTION
    # FLY_API_TOKEN is an organization-wide secret (accessible regardless of environment)
    # All other secrets are from the PRODUCTION environment
    env:
      FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
    steps:
      - uses: actions/checkout@v4

      - uses: superfly/flyctl-actions/setup-flyctl@master

      - name: Create Fly.io app if it doesn't exist
        run: |
          # Check if app exists, create if it doesn't
          if ! flyctl apps list | grep -q "hominio-wallet"; then
            echo "📦 Creating Fly.io app: hominio-wallet..."
            flyctl apps create hominio-wallet --org personal || {
              echo "⚠️  Failed to create app. It may already exist or you need to specify a different org."
              echo "   Continuing with deployment - app may need to be created manually."
            }
            
            # Set primary region
            flyctl regions set fra --app hominio-wallet || true
            
            # Allocate IP addresses
            echo "🌐 Allocating IP addresses..."
            flyctl ips allocate-v4 --app hominio-wallet || true
            flyctl ips allocate-v6 --app hominio-wallet || true
          else
            echo "✅ App hominio-wallet already exists"
          fi

      - name: Configure custom domain (wallet.hominio.me)
        run: |
          echo "🌐 Configuring custom domain: wallet.hominio.me"
          # Add the custom domain to Fly.io (idempotent - safe to run multiple times)
          flyctl certs create wallet.hominio.me --app hominio-wallet || {
            echo "⚠️  Certificate may already exist or domain already configured"
            echo "   Checking certificate status..."
            flyctl certs list --app hominio-wallet || true
          }
          
          echo ""
          echo "📋 DNS Configuration Required:"
          echo "   Add a CNAME record in your DNS provider:"
          echo "   Type: CNAME"
          echo "   Name: wallet"
          echo "   Value: hominio-wallet.fly.dev"
          echo "   TTL: 300 (or your preferred TTL)"
          echo ""
          echo "   After DNS propagates, Fly.io will automatically provision Let's Encrypt certificate."

      - name: Set Fly.io secrets for wallet service
        run: |
          # Set all required environment variables from GitHub secrets
          # PUBLIC_ vars are public and accessible in both client and server
          # Non-PUBLIC vars are secret and only accessible on the server
          flyctl secrets set \
            PUBLIC_DOMAIN_ROOT="${{ secrets.PUBLIC_DOMAIN_ROOT }}" \
            PUBLIC_DOMAIN_APP="${{ secrets.PUBLIC_DOMAIN_APP }}" \
            PUBLIC_DOMAIN_WALLET="${{ secrets.PUBLIC_DOMAIN_WALLET }}" \
            PUBLIC_DOMAIN_SYNC="${{ secrets.PUBLIC_DOMAIN_SYNC }}" \
            AUTH_SECRET="${{ secrets.AUTH_SECRET }}" \
            GOOGLE_CLIENT_ID="${{ secrets.GOOGLE_CLIENT_ID }}" \
            GOOGLE_CLIENT_SECRET="${{ secrets.GOOGLE_CLIENT_SECRET }}" \
            WALLET_POSTGRES_SECRET="${{ secrets.WALLET_POSTGRES_SECRET }}" \
            POLAR_API_KEY="${{ secrets.POLAR_API_KEY }}" \
            NODE_ENV="production" \
            --app hominio-wallet

          echo "✅ Wallet service secrets set. Verifying..."
          flyctl secrets list --app hominio-wallet

      - name: Deploy wallet service
        run: |
          cd services/wallet
          flyctl deploy --remote-only --app hominio-wallet

      - name: Check deployment status
        run: |
          flyctl status --app hominio-wallet
          echo "📋 Recent logs:"
          flyctl logs --app hominio-wallet --no-tail | tail -50
          
      - name: Verify SSL certificate status
        run: |
          echo "🔒 Checking SSL certificate status for wallet.hominio.me..."
          flyctl certs check wallet.hominio.me --app hominio-wallet || {
            echo "⚠️  Certificate not yet issued. This is normal if DNS hasn't propagated yet."
            echo "   Certificate will be automatically provisioned once DNS CNAME record is configured."
          }

