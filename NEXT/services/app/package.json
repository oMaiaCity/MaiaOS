{
	"name": "@MaiaOS/app",
	"version": "0.0.0",
	"type": "module",
	"private": true,
	"scripts": {
		"build:client": "bun build ./client.js --outfile ./dist/client.mjs --target browser --format esm",
		"dev": "bun run build:client && bun run dev.js",
		"start": "bun run build:client && bun run server.js"
	}
}
