{
  "name": "@maia/script",
  "version": "0.17.0",
  "type": "module",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./modules/*": "./src/modules/*"
  },
  "scripts": {
    "dev": "bun run dev:docs & bunx browser-sync start --server src --port 4200 --files 'src/**/*.{js,maia,html,css}' --no-notify --no-open --no-ui",
    "dev:docs": "bun run scripts/generate-llm-docs.js --watch",
    "build:docs": "bun run scripts/generate-llm-docs.js",
    "serve": "bunx serve -p 4200 src",
    "format": "bunx biome format --write .",
    "lint": "bunx biome lint .",
    "check": "bunx biome check ."
  }
}
