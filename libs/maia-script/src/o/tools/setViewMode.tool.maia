{
  "$type": "tool",
  "$id": "tool_setViewMode_001",
  "name": "@core/setViewMode",
  "description": "Sets the view mode (list or kanban)",
  "parameters": {
    "type": "object",
    "properties": {
      "viewMode": {
        "type": "string",
        "enum": ["list", "kanban"],
        "description": "The view mode to set"
      }
    },
    "required": ["viewMode"]
  }
}
