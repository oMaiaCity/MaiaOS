/**
 * Re-export sync timeouts from the shared package (single source of truth).
 */

export {
	TIMEOUT_COVALUE_LOAD,
	TIMEOUT_HTTP,
	TIMEOUT_STORAGE_PERSIST,
	TIMEOUT_WS_CONNECT,
} from '@MaiaOS/timeouts'
