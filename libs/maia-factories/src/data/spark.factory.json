{
	"$factory": "°Maia/factory/meta",
	"$id": "°Maia/factory/data/spark",
	"title": "°Maia/factory/data/spark",
	"description": "Spark CoMap. Guardian (admin) group is spark.os.groups.guardian.",
	"cotype": "comap",
	"indexing": true,
	"required": ["name"],
	"properties": {
		"name": { "type": "string", "description": "Spark name e.g. °Maia" },
		"os": {
			"type": "string",
			"pattern": "^co_z[a-zA-Z0-9]+$",
			"description": "OS CoMap - contains factories, indexes, groups (guardian, publicReaders)."
		},
		"vibes": {
			"type": "string",
			"pattern": "^co_z[a-zA-Z0-9]+$",
			"description": "Vibes registry CoMap for this spark."
		}
	}
}
