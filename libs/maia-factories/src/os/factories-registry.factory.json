{
	"$factory": "°maia/factory/meta",
	"title": "°maia/factory/os/factories-registry",
	"description": "Schema registry CoMap - maps schema namekeys (e.g., °maia/factory/data/todos) to schema co-ids (co_z...). Stored in account.os.factories",
	"cotype": "comap",
	"indexing": false,
	"properties": {
		"°maia/factory/meta": {
			"type": "string",
			"description": "Metaschema co-id (co_z...)"
		}
	},
	"additionalProperties": {
		"type": "string",
		"description": "Schema namekey → schema co-id mapping (e.g., °maia/factory/data/todos → co_z123...)"
	}
}
