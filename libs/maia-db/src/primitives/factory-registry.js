/**
 * Schema Registry — re-exports peer-backed metaschema from @MaiaOS/validation only.
 *
 * `loadFactoriesFromAccount` is bundled in {@link ../modules/cojson-impl.js} and exported from `@MaiaOS/db`.
 */

export * from '@MaiaOS/validation/peer-factory-registry'
