/**
 * Schema Registry — re-exports peer-backed metaschema from @MaiaOS/validation only.
 *
 * `loadFactoriesFromAccount` lives in {@link ../cojson/factory/load-factories-from-account.js} and is exported from `@MaiaOS/db` main entry (via `./cojson/factory/resolver.js`).
 */

export * from '@MaiaOS/validation/peer-factory-registry'
