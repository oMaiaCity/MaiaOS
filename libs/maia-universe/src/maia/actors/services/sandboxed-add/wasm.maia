{
	"$factory": "°maia/factory/os/wasm",
	"lang": "js",
	"code": "({ execute: function(actor, payload) { var a = Number(payload.a) || 0; var b = Number(payload.b) || 0; return { ok: true, data: { result: a + b } }; } })"
}
