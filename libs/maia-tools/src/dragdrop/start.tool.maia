{
  "$schema": "@schema/tool",
  "$id": "tool_dragdrop_start_001",
  "name": "@dragdrop/start",
  "description": "Generic drag start handler. Stores the dragged entity information in context for later use by drop handler. Schema is extracted from CoValue headerMeta using universal schema resolver.",
  "parameters": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "The entity ID being dragged"
      }
    },
    "required": ["id"]
  }
}
