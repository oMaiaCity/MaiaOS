/**
 * Profile image view — same native module as @maia/actor/services/profile-image
 * (upload avatar + OPFS reset). ExecutableKey: maia/actor/views/profile-image
 */
export { default } from '../../services/profile-image/function.js'
