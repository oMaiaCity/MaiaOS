{
  "$id": "@schema/data/todos",
  "$schema": "@schema/meta",
  "title": "@schema/data/todos",
  "cotype": "comap",
  "properties": {
    "text": {
      "type": "string",
      "minLength": 1,
      "description": "The todo item text content"
    },
    "done": {
      "type": "boolean",
      "description": "Whether the todo item is completed"
    }
  },
  "required": ["text", "done"],
  "additionalProperties": false
}
