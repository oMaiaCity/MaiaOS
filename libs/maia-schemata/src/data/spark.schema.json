{
	"$schema": "°Maia/schema/meta",
	"$id": "°Maia/schema/data/spark",
	"title": "°Maia/schema/data/spark",
	"description": "Spark CoMap. Guardian (admin) group is spark.os.groups.guardian.",
	"cotype": "comap",
	"indexing": true,
	"required": ["name"],
	"properties": {
		"name": { "type": "string", "description": "Spark name e.g. °Maia" },
		"os": {
			"type": "string",
			"pattern": "^co_z[a-zA-Z0-9]+$",
			"description": "OS CoMap - contains schematas, indexes, groups (guardian, publicReaders)."
		},
		"agents": {
			"type": "string",
			"pattern": "^co_z[a-zA-Z0-9]+$",
			"description": "Agents registry CoMap for this spark."
		}
	}
}
