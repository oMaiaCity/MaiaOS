{
  "$schema": "@schema/meta",
  "$id": "@schema/os/index-colist",
  "title": "@schema/os/index-colist",
  "description": "Schema index colist - contains co-ids of all instances of a specific schema. Stored in account.os.{schemaCoId}",
  "cotype": "colist",
  "items": {
    "type": "string",
    "description": "Co-id of an instance (co_z...)"
  }
}
