{
  "$schema": "@schema/meta",
  "$id": "@schema/message",
  "title": "@schema/message",
  "description": "Actor message for state machine transitions",
  "cotype": "comap",
  "indexing": true,
  "required": ["type"],
  "properties": {
    "type": {
      "type": "string",
      "description": "Message type (event name)"
    },
    "payload": {
      "type": "object",
      "description": "Message payload data"
    },
    "source": {
      "$co": "@schema/actor",
      "description": "Co-id reference to source actor"
    },
    "target": {
      "$co": "@schema/actor",
      "description": "Co-id reference to target actor (optional, bypasses topic routing if present)"
    },
    "topic": {
      "$co": "@schema/topic",
      "description": "Co-id reference to topic CoValue for routing (e.g., '@topic/todos-created')"
    },
    "processed": {
      "type": "boolean",
      "description": "Whether this message has been processed",
      "default": false
    }
  }
}
