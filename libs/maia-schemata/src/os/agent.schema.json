{
	"$schema": "°Maia/schema/meta",
	"$id": "°Maia/schema/agent",
	"title": "°Maia/schema/agent",
	"description": "Agent manifest/metadata",
	"cotype": "comap",
	"indexing": true,
	"required": ["name", "description", "actor"],
	"properties": {
		"name": { "type": "string", "description": "Agent name" },
		"description": { "type": "string", "description": "Agent description" },
		"actor": {
			"$co": "°Maia/schema/actor",
			"description": "Co-id reference to actor definition (root actor for this agent)"
		},
		"dependencies": {
			"type": "array",
			"items": { "type": "string" },
			"description": "Explicit actor $ids whose inboxes must be watched (service actors this agent sends to)"
		},
		"runtime": {
			"type": "array",
			"items": { "type": "string" },
			"description": "Runtime colocation - which runtimes this agent runs on (e.g. browser, server)"
		}
	}
}
