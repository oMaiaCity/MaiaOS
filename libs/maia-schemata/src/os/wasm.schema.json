{
	"$id": "°Maia/schema/os/wasm",
	"$schema": "°Maia/schema/meta",
	"title": "°Maia/schema/os/wasm",
	"cotype": "comap",
	"properties": {
		"lang": {
			"type": "string",
			"enum": ["js"],
			"default": "js",
			"description": "Execution language: 'js' = run in QuickJS WASM sandbox"
		},
		"code": {
			"$co": "°Maia/schema/os/cotext",
			"description": "CoText (colist of graphemes) - JS code to execute in sandbox"
		}
	},
	"required": ["lang", "code"],
	"additionalProperties": false
}
