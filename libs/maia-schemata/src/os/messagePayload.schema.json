{
  "$schema": "@schema/meta",
  "$id": "@schema/messagePayload",
  "title": "Message Payload",
  "description": "Message payload definition",
  "cotype": "comap",
  "properties": {
    "$id": {
      "type": "string",
      "description": "Unique message payload identifier (co-id)",
      "pattern": "^co_z[a-zA-Z0-9]+$"
    }
  },
  "additionalProperties": true
}
