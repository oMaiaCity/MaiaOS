{
  "$schema": "@schema/meta",
  "$id": "@schema/topic",
  "title": "Topic CoValue",
  "description": "A topic CoValue representing a message category/channel. Contains subscribers list and optional schema for validation.",
  "cotype": "comap",
  "properties": {
    "name": {
      "type": "string",
      "description": "Topic name (e.g., 'todos.created', 'todos.deleted')"
    },
    "subscribers": {
      "$co": "@schema/subscribers",
      "description": "Co-id reference to subscribers colist (list of actor co-ids subscribed to this topic)"
    },
    "schema": {
      "$co": "@schema/message",
      "description": "Optional: Co-id reference to message schema for validation"
    },
    "metadata": {
      "type": "object",
      "description": "Optional: Topic metadata (description, namespace, etc.)",
      "additionalProperties": true
    }
  }
}
