{
  "$schema": "@schema/meta",
  "$id": "@schema/transition",
  "title": "@schema/transition",
  "description": "State machine transition",
  "cotype": "comap",
  "indexing": true,
  "properties": {
    "target": {
      "type": "string",
      "description": "Target state name"
    },
    "guard": {
      "$co": "@schema/guard"
    },
    "actions": {
      "type": "array",
      "items": {
        "$co": "@schema/action"
      }
    }
  },
  "required": ["target"]
}
