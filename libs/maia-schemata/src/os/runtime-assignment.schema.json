{
	"$schema": "°Maia/schema/meta",
	"$id": "°Maia/schema/os/runtime-assignment",
	"title": "°Maia/schema/os/runtime-assignment",
	"description": "Single runtime assignment: browser (account/group co-id) or server (agent account co-id)",
	"cotype": "comap",
	"indexing": false,
	"oneOf": [
		{
			"required": ["browser"],
			"properties": {
				"browser": {
					"type": "string",
					"pattern": "^co_z[a-zA-Z0-9]+$",
					"description": "Account or group co-id that runs this vibe in browser"
				}
			}
		},
		{
			"required": ["server"],
			"properties": {
				"server": {
					"type": "string",
					"pattern": "^co_z[a-zA-Z0-9]+$",
					"description": "Agent account co-id that runs this vibe on server"
				}
			}
		}
	]
}
