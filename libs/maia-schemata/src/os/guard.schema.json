{
  "$schema": "@schema/meta",
  "$id": "@schema/guard",
  "title": "Guard",
  "description": "Guard condition for state machine transitions",
  "cotype": "comap",
  "properties": {
    "$id": {
      "type": "string",
      "description": "Unique guard identifier (co-id)",
      "pattern": "^co_z[a-zA-Z0-9]+$"
    }
  },
  "additionalProperties": true
}
