{
	"$schema": "°Maia/schema/meta",
	"$id": "°Maia/schema/event/LOAD_ACTOR",
	"title": "°Maia/schema/event/LOAD_ACTOR",
	"description": "Generic message type for loading actor data (reusable across agents)",
	"cotype": "comap",
	"indexing": true,
	"type": "object",
	"properties": {
		"id": {
			"type": "string",
			"description": "Entity co-id to load (required, generic property name for reuse across agents)"
		}
	},
	"required": ["id"],
	"additionalProperties": true
}
