{
	"$schema": "°Maia/schema/meta",
	"$id": "°Maia/schema/event/SWITCH_VIEW",
	"title": "°Maia/schema/event/SWITCH_VIEW",
	"description": "Message type for switching view mode",
	"cotype": "comap",
	"indexing": true,
	"type": "object",
	"properties": {
		"viewMode": {
			"type": "string",
			"enum": ["list", "logs", "placeholder", "actors", "schemata"],
			"description": "View mode to switch to (required)"
		},
		"slotRef": {
			"type": "string",
			"description": "Slot reference for the view (e.g. @logs, @actors). Used by generic tab switchers."
		}
	},
	"required": ["viewMode"],
	"additionalProperties": false
}
