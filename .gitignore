node_modules

# Output
.output
.vercel
.netlify
.wrangler

# SvelteKit generated files (all services)
**/.svelte-kit/
.svelte-kit/

# Build outputs (all services)
**/build/
build/

# Rust build artifacts (Tauri)
**/src-tauri/target/
**/src-tauri/gen/
# Exception: iOS version files need to be tracked for version sync
!**/src-tauri/gen/apple/app_iOS/Info.plist
!**/src-tauri/gen/apple/project.yml

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Database files (SQLite, Zero replica, etc.)
*.db
*.db-shm
*.db-wal
*.db-wal2
**/zero-replica.db*
**/*.db-journal

# PGlite local dev data (moai sync server - never commit)
**/pgdata/
local-sync.db
/tmp/pglite/

# Legacy maia-city (removed - use services/maia)
services/maia-city/

# Brand assets (synced from libs/maia-brand/src/assets to services/maia/public/brand)
**/static/brand/
**/public/brand/
**/node_modules

debug.log*

# LLM-generated docs (auto-generated by docs:generate, never commit)
libs/maia-docs/04_agents/LLM_*.md

# Kernel and vibes bundles (built during Docker/production build only, not used in dev)
libs/maia-kernel/dist/
libs/maia-vibes/dist/