node_modules

# Local research clones
repos/

# Output
.output
.vercel
.netlify
.wrangler

# SvelteKit generated files (all services)
**/.svelte-kit/
.svelte-kit/

# Build outputs (all services)
**/build/
build/

# Rust build artifacts (Tauri)
**/src-tauri/target/
**/src-tauri/gen/

# Tauri app icons: generated by `bun run tauri:icon` from services/app/brand/logo_app.svg (see services/app package.json)
services/app/src-tauri/icons/

# maia-tauri-plugin-passkey: generated by build.rs `tauri_plugin::Builder` on cargo build
libs/maia-tauri-plugin-passkey/permissions/schemas/schema.json
libs/maia-tauri-plugin-passkey/permissions/autogenerated/
# Exception: iOS version files need to be tracked for version sync
!**/src-tauri/gen/apple/app_iOS/Info.plist
!**/src-tauri/gen/apple/project.yml

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Database files (SQLite, Zero replica, etc.)
*.db
*.db-shm
*.db-wal
*.db-wal2
**/zero-replica.db*
**/*.db-journal

# PGlite local dev data (sync server — never commit)
**/pgdata/
pg-lite.db
**/pg-lite.db
/tmp/pglite/

# Binary blob bucket (CoValue offloading - dev only, never commit)
binary-bucket/
**/binary-bucket/

# Legacy (removed - use services/app)
services/maia/
services/maia-city/

# Legacy NEXT/ offline scaffold (removed from repo; main app is services/app)
NEXT/

# Brand: `services/app/brand/` is tracked; sync script may refresh from libs/maia-brand/src/assets
**/static/brand/
services/app/public/
**/node_modules

debug.log*

# @AvenOS/kernel bundles + wasm (built in Docker/CI; local `bun run distros:build`)
libs/kernel/output/

# Generated universe registry (regenerated in CI, Docker build, and dev)
libs/maia-universe/src/generated/
libs/maia-universe/src/registry.js

# maia-universe dist (build output, not used in dev - we use source)
libs/maia-universe/dist/

# maia-game bundled terrain worker (built by build:terrain-worker or app build.js)
libs/maia-game/dist/

# Cursor / agent skills local lock (optional tooling output)
skills-lock.json

# Removed Moai + maia-ai from repo — ignore leftover dirs if present locally
services/moai/